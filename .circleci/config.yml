# jobs:
#   build:
#     docker:
#       - image: node:18.13.0

#     steps:
#       - checkout:
#       - run:
#           name: Install dependencies
#           command: npm install
#       - run:
#           name: Run tests
#           command: npm test --coverage
#           - run:
#           name: Upload coverage to Coveralls
#           command: npx coveralls < coverage/lcov.info
# notify:
#   webhooks:
#     - url: https://coveralls.io/webhook?repo_token=${process.env.COVERALLS_REPO_TOKEN}
#     environment:

#    COVERALLS_REPO_TOKEN:

version: 2.1
jobs:
  build:
    docker:
      - image: node:18.13.0
    environment:
      COVERALLS_REPO_TOKEN: MLbOdVJKlIjgo0gCtvxFjrXWz7JWOqJQ0
    working_directory: ~/nodejs-endpoint
    steps:
      - checkout:
          path: ~/nodejs-endpoint
      - run:
          name: Install dependencies
          command: npm install
      - run:
          name: Run tests
          command: npm test --coverage -- --verbose
      - run:
          name: Upload coverage to Coveralls
          command: npx coveralls < coverage/lcov.info
